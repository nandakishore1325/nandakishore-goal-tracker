# ===========================================
# Firebase Configuration
# ===========================================
# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com
# 2. Create a new project or select existing
# 3. Go to Project Settings > General > Your apps
# 4. Click "Add app" > Web app
# 5. Copy the config values below

VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# ===========================================
# Google APIs (Calendar & Gmail)
# ===========================================
# The app uses Firebase Auth with Google Sign-in
# Additional scopes for Calendar and Gmail are requested at sign-in
#
# To enable Google Calendar & Gmail sync:
# 1. In Firebase Console, go to Authentication > Sign-in method
# 2. Enable Google sign-in
# 3. In Google Cloud Console, enable:
#    - Google Calendar API
#    - Gmail API
# 4. Configure OAuth consent screen

# ===========================================
# Slack Integration (Optional)
# ===========================================
# To enable Slack integration:
# 1. Go to https://api.slack.com/apps
# 2. Create a new Slack App
# 3. Add OAuth scopes: channels:history, channels:read, users:read, reactions:read
# 4. Set redirect URL to your app's callback URL
# 5. Copy the Client ID below

VITE_SLACK_CLIENT_ID=your-slack-client-id
VITE_SLACK_REDIRECT_URI=http://localhost:5173/auth/slack/callback

# Note: Slack OAuth token exchange requires a backend server
# for security (to protect client_secret). The current implementation
# shows a placeholder - you'll need to set up a backend endpoint
# to complete the OAuth flow.
